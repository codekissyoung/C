#include <stdio.h>
#include <utmp.h>
#include <fcntl.h>
#include <unistd.h>
#include <stdlib.h>
#include <sys/types.h>

void insert_sort(int arr[], int len);
void merge_sort(int a[], int first, int last);
